{
  "name": "unityverse-signaling-server",
  "version": "1.0.0",
  "description": "WebRTC signaling server for UnityVerse Bridge",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:auth": "cross-env REQUIRE_AUTH=true AUTH_KEY=development-key node server.js",
    "start:prod": "cross-env REQUIRE_AUTH=true node server.js"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express-rate-limit": "^7.5.0",
    "ws": "^8.13.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
